/* update the value of node to be deleted with the max possible priority value (in case of max heap) and with min possible priority value (in case of min heap)
perculate it up in order until it becmoes the root node
swap root with the last node
pop out the last node
apply downheapify to make the heap
update the array */
